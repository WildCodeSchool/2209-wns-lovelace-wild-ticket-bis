import { StatusBar } from 'expo-status-bar';
import { View, Text } from 'react-native';

const PageClient = () => {
  return (
    <View>
      <Text>PageClient</Text>
      <StatusBar style="auto" />
    </View>
  );
};

export default PageClient;
